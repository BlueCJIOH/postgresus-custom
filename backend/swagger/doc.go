// Package swagger provides the statically compiled OpenAPI specification for the
// Postgresus backend. The generated files can be refreshed by running
// `make swagger`, but we commit a minimal specification to ensure the
// application builds and serves placeholder documentation even before the
// generator runs.
package swagger

